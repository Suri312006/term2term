use std::env::home_dir;

use xdg_home::home_dir;

fn initialize(){

    let err_msg = "Unable to find your HOME directory. \
        Consider setting $HOME to your home directory and try again." ;
    // let home = home_dir().unwrap_or(|| err)
    //     consider""")



}
